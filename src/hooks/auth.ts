// Re-export authentication components and hooks
import { useAuth } from '@/components/auth'

// Export for convenient usage throughout the app
export { useAuth }

// Add additional auth-related hooks here if needed 